#ifndef C_DEFINITIONS_H
#define C_DEFINITIONS_H

char *c_definitions[] = {
    "#include <stdint.h>\n#include <stdio.h>\n#include <stdlib.h>\nint main(int argc, char** argv)\n{\n\tint16_t mem[4096];\n\tint16_t flag = 0;\n",
    "\tmem[%d]=%d;\n",
    "\tmem[%d]=mem[%d];\n",
    "\tflag=(%d<%d)*(-1)+(%d>%d)*1;\n",
    "\tflag=(%d<mem[%d])*(-1)+(%d>mem[%d])*1;\n",
    "\tflag=(mem[%d]<%d)*(-1)+(mem[%d]>%d)*1;\n",
    "\tflag=(mem[%d]<mem[%d])*(-1)+(mem[%d]>mem[%d])*1;\n",
    "\tgoto %s;\n",
    "\tif(flag!=0)goto %s;\n",
    "\tif(flag==0)goto %s;\n",
    "\tif(flag>0)goto %s;\n",
    "\tif(flag<0)goto %s;\n",
    "\tmem[%d] = %d;\n",
    "\tmem[%d] = %d+mem[%d];\n",
    "\tmem[%d] = mem[%d]+%d;\n",
    "\tmem[%d] = mem[%d]+mem[%d];\n",
    "\tmem[%d] = %d;\n",
    "\tmem[%d] = %d-mem[%d];\n",
    "\tmem[%d] = mem[%d]-%d;\n",
    "\tmem[%d] = mem[%d]-mem[%d];\n",
    "\tmem[%d] = %d;\n",
    "\tmem[%d] = %d*mem[%d];\n",
    "\tmem[%d] = mem[%d]*%d;\n",
    "\tmem[%d] = mem[%d]*mem[%d];\n",
    "\tmem[%d] = %d;\n",
    "\tmem[%d] = %d/mem[%d];\n",
    "\tmem[%d] = mem[%d]/%d;\n",
    "\tmem[%d] = mem[%d]/mem[%d];\n",
    "\tmem[%d] = %d;\n",
    "\tmem[%d] = %d&mem[%d];\n",
    "\tmem[%d] = mem[%d]&%d;\n",
    "\tmem[%d] = mem[%d]&mem[%d];\n",
    "\tmem[%d] = %d;\n",
    "\tmem[%d] = %d|mem[%d];\n",
    "\tmem[%d] = mem[%d]|%d;\n",
    "\tmem[%d] = mem[%d]|mem[%d];\n",
    "\tmem[%d] = %d;\n",
    "\tmem[%d] = %d^mem[%d];\n",
    "\tmem[%d] = mem[%d]^%d;\n",
    "\tmem[%d] = mem[%d]^mem[%d];\n",
    "\tmem[%d] = %d;\n",
    "\tmem[%d] = ~mem[%d];\n",
    "\tprintf(\"%%s\", %s);\n",
    "\tprintf(%s,%d);\n",
    "\tprintf(%s,mem[%d]);\n",
    "\tprintf(%s,\'%c\');\n",
    "\tprintf(%s,%s);\n",
    "%s\n",
    "return;\n",
    "}\n"};

#endif // C_DEFINITIONS_H
